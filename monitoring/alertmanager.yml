global:
  resolve_timeout: 5m

route:
  receiver: 'telegram'
  group_wait: 10s
  group_interval: 5m
  repeat_interval: 30m

receivers:
  - name: 'telegram'
    webhook_configs:
      - url: 'http://telegram-bot:8080/alert'
        send_resolved: true
